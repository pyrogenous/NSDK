# JitPack builds the library at the root of this repository: com.github.<user>:<repo>:<tag>
# Gradle downloads the JDK 25 the build needs through its toolchain settings.
jdk:
  - openjdk21
install:
  - ./gradlew publishToMavenLocal
