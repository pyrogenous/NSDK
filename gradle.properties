org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=true
# Off: several Forge builds setting up their Minecraft at once break each other
org.gradle.parallel=false
org.gradle.caching=true

# Version of every artifact this repository publishes (overridden by NITEA_VERSION in CI)
nitea_version=0.4.0
